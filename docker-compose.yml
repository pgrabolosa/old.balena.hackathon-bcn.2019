version: '2'
services:
#  sensorhat:
#    build: ./sensehat
#    privileged: true
#    restart: always
#    ports:
#      - "5000:5000" 
  display:
    build: ./display
    privileged: true
    restart: always

  joystick:
    build: ./joystick
    privileged: true
    restart: always

  mosquitto:
    build: mosquitto
    restart: always
    expose:
      - "1883"
